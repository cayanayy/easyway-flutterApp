import 'package:flutter/material.dart';

class LeftOrRight extends StatelessWidget {
  const LeftOrRight({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('left or right screen'),
    );
  }
}
