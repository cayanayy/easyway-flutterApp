import 'package:flutter/material.dart';

class FlipCoin extends StatelessWidget {
  const FlipCoin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('flip coin screen'),
    );
  }
}
