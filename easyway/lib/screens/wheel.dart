import 'package:flutter/material.dart';

class Wheel extends StatelessWidget {
  const Wheel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('wheel screen'),
    );
  }
}
